kind = "web"
previewPath = "/"
title = "Bookmark Manager"
version = "1.0.0"
id = "artifacts/bookmark-manager"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24728

[services.development]
run = "pnpm --filter @workspace/bookmark-manager run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/bookmark-manager", "run", "build" ]
publicDir = "artifacts/bookmark-manager/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24728"
BASE_PATH = "/"
